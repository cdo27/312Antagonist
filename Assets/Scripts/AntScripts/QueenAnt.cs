using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QueenAnt : NPCAnt
{
    // Start is called before the first frame update
    void Start()
    {
        npcType = NPCAnt.NPCType.QueenAnt;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
